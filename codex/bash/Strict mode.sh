# bash, pipefail, set, strict mode, echo


set -euo pipefail

# set -e            exit on error (immediately)
# set -u            exit on unset variable
# set -o pipefail   exit on a pipefail

# set -x            echo commands as they run
