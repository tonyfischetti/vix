// js, strings, substring, slice

const str = "Hello";

str.substring(0, 4);  // "Hell"
str.substring(3);     // "lo"
// str.substr takes a start and a length

str.slice(0, -1);     // "Hell"
str.slice(-2);        // "lo"
