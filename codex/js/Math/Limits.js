// js, math, special numbers, limits, epsilon, max safe integer, min safe integer
Number.EPSILON;            // Diff between 1 and smalled number above 1

Number.MAX_SAFE_INTEGER;
// 9007199254740991 (2**53 – 1) [last non BigInt]
// after this point, you'll have round-off errors

// But you can use `BigInt`s that can have an arbitrary number of digits

Number.MAX_VALUE;          // 1.7976931348623157e+308
Number.MIN_SAFE_INTEGER;
Number.MIN_VALUE;
