
return {

  {
    'LudoPinelli/comment-box.nvim',
    -- format
    -- CBxyz n
    -- where `x` ∈ { "l", "c", "r" } (left, center, right)
    -- where `y` ∈ { "l", "c", "r", "a" } (..., adapted)
    -- where `z` ∈ { "box", "line" }
    -- where `n` is a style from the catalog
    --  TODO: list favorite styles
  }

}

